[project]
name = "aibis"
version = "0.1.0"
description = "AI-Based Incident System for cloud-native environments"
requires-python = ">=3.12"

dependencies = [
    "kubernetes>=30.1.0",
    "langchain>=1.0.0",
    "langchain-openai>=1.0.0",
    "python-dotenv>=1.0.1",
]